{
  "name": "job-portal-monorepo",
  "version": "1.0.0",
  "description": "Unified Job Portal Project",
  "scripts": {
    "build": "npm install --prefix Backend && npm install --prefix Frontend-job-portal-client-side && npm run build --prefix Frontend-job-portal-client-side",
    "start": "node Backend/server.js",
    "dev-backend": "npm run dev --prefix Backend",
    "dev-frontend": "npm run dev --prefix Frontend-job-portal-client-side"
  }
}
